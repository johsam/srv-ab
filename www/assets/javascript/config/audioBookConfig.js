/*
 *  Created by: Johan S - 2017
 *
 */

/*eslint no-unused-vars: ["error", { "varsIgnorePattern": "abConfig" }] */

// @flow

const abConfig = {
    settings: {
        winMargin: 5, //  Amount of edge around the main window
        urls: {
            ab: 'audiobooks/load/audiobooks',
        },
    },
};
